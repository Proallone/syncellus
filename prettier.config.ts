import { type Config } from "prettier";

const config: Config = {
    trailingComma: "none",
    tabWidth: 4,
    printWidth: 200,
    semi: true,
    singleQuote: false,
    
};

export default config;
