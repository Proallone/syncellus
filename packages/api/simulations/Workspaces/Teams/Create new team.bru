meta {
  name: Create new team
  type: http
  seq: 3
}

post {
  url: http://{{host}}:{{port}}/{{api_prefix}}/workspaces/teams
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{$randomDomainWord}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
