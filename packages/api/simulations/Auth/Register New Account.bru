meta {
  name: Register New Account
  type: http
  seq: 1
}

post {
  url: http://{{host}}:{{port}}/{{api_prefix}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email" : "{{$randomEmail}}",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
