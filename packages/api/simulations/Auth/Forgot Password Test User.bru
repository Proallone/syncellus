meta {
  name: Forgot Password Test User
  type: http
  seq: 5
}

post {
  url: http://{{host}}:{{port}}/{{api_prefix}}/auth/forgot-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "bartek@manager.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
